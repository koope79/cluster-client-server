#JSGF V1.0;

grammar cmdGrammar;

public <cmdGrammar> = <start> <cmdCamerа>;
<cmdCamerа> = <actioncamera> <camera> <pretext> <locationCamera>;
<actioncamera> = (включи | выключи);
<camera> = (камеру);
<pretext> = (в);
<locationCamera> = (зале | комнате | прихожей);
<start> = [умный дом | дом];
