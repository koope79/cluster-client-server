#JSGF V1.0;

grammar cmdGrammar;

public <basicCmd> = <start> <cmdHeater>;
<cmdHeater> = <heater> <actionHeater> <parameterHeater>;
<heater> = (обогреватель);
<actionHeater> = (сделай);
<parameterHeater> = (горячее|холоднее);
<start> = [умный дом | дом];